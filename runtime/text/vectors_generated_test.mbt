// Generated by tools/generate-runtime.mjs using Z3; do not edit.

///|
test "Z3 Unicode case 0" {
  let text = @text.from_string("").unwrap()
  assert_eq(text.length(), 0)
  assert_eq(text.char_at(-1).to_string(), "")
  assert_eq(text.substring(-1, 1).to_string(), "")
}

///|
test "Z3 Unicode case 1" {
  let text = @text.from_string("").unwrap()
  assert_eq(text.length(), 0)
  assert_eq(text.char_at(0).to_string(), "")
  assert_eq(text.substring(0, 0).to_string(), "")
}

///|
test "Z3 Unicode case 2" {
  let text = @text.from_string("").unwrap()
  assert_eq(text.length(), 0)
  assert_eq(text.char_at(0).to_string(), "")
  assert_eq(text.substring(0, 1).to_string(), "")
}

///|
test "Z3 Unicode case 3" {
  let text = @text.from_string("").unwrap()
  assert_eq(text.length(), 0)
  assert_eq(text.char_at(1).to_string(), "")
  assert_eq(text.substring(1, 2).to_string(), "")
}

///|
test "Z3 Unicode case 4" {
  let text = @text.from_string("").unwrap()
  assert_eq(text.length(), 0)
  assert_eq(text.char_at(0).to_string(), "")
  assert_eq(text.substring(0, -1).to_string(), "")
}

///|
test "Z3 Unicode case 5" {
  let text = @text.from_string("").unwrap()
  assert_eq(text.length(), 0)
  assert_eq(text.char_at(0).to_string(), "")
  assert_eq(text.substring(0, 1).to_string(), "")
}

///|
test "Z3 Unicode case 6" {
  let text = @text.from_string("").unwrap()
  assert_eq(text.length(), 0)
  assert_eq(text.char_at(-1).to_string(), "")
  assert_eq(text.substring(-1, 2147483647).to_string(), "")
}

///|
test "Z3 Unicode case 7" {
  let text = @text.from_string("").unwrap()
  assert_eq(text.length(), 0)
  assert_eq(text.char_at(2147483647).to_string(), "")
  assert_eq(text.substring(2147483647, 1).to_string(), "")
}

///|
test "Z3 Unicode case 8" {
  let text = @text.from_string("\u{61}\u{62}\u{63}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(-1).to_string(), "")
  assert_eq(text.substring(-1, 1).to_string(), "")
}

///|
test "Z3 Unicode case 9" {
  let text = @text.from_string("\u{61}\u{62}\u{63}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(0).to_string(), "\u{61}")
  assert_eq(text.substring(0, 0).to_string(), "")
}

///|
test "Z3 Unicode case 10" {
  let text = @text.from_string("\u{61}\u{62}\u{63}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(0).to_string(), "\u{61}")
  assert_eq(text.substring(0, 1).to_string(), "\u{61}")
}

///|
test "Z3 Unicode case 11" {
  let text = @text.from_string("\u{61}\u{62}\u{63}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(1).to_string(), "\u{62}")
  assert_eq(text.substring(1, 2).to_string(), "\u{62}\u{63}")
}

///|
test "Z3 Unicode case 12" {
  let text = @text.from_string("\u{61}\u{62}\u{63}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(0).to_string(), "\u{61}")
  assert_eq(text.substring(0, -1).to_string(), "")
}

///|
test "Z3 Unicode case 13" {
  let text = @text.from_string("\u{61}\u{62}\u{63}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(3).to_string(), "")
  assert_eq(text.substring(3, 1).to_string(), "")
}

///|
test "Z3 Unicode case 14" {
  let text = @text.from_string("\u{61}\u{62}\u{63}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(2).to_string(), "\u{63}")
  assert_eq(text.substring(2, 2147483647).to_string(), "\u{63}")
}

///|
test "Z3 Unicode case 15" {
  let text = @text.from_string("\u{61}\u{62}\u{63}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(2147483647).to_string(), "")
  assert_eq(text.substring(2147483647, 1).to_string(), "")
}

///|
test "Z3 Unicode case 16" {
  let text = @text.from_string("\u{41}\u{1f600}\u{e9}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(-1).to_string(), "")
  assert_eq(text.substring(-1, 1).to_string(), "")
}

///|
test "Z3 Unicode case 17" {
  let text = @text.from_string("\u{41}\u{1f600}\u{e9}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(0).to_string(), "\u{41}")
  assert_eq(text.substring(0, 0).to_string(), "")
}

///|
test "Z3 Unicode case 18" {
  let text = @text.from_string("\u{41}\u{1f600}\u{e9}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(0).to_string(), "\u{41}")
  assert_eq(text.substring(0, 1).to_string(), "\u{41}")
}

///|
test "Z3 Unicode case 19" {
  let text = @text.from_string("\u{41}\u{1f600}\u{e9}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(1).to_string(), "\u{1f600}")
  assert_eq(text.substring(1, 2).to_string(), "\u{1f600}\u{e9}")
}

///|
test "Z3 Unicode case 20" {
  let text = @text.from_string("\u{41}\u{1f600}\u{e9}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(0).to_string(), "\u{41}")
  assert_eq(text.substring(0, -1).to_string(), "")
}

///|
test "Z3 Unicode case 21" {
  let text = @text.from_string("\u{41}\u{1f600}\u{e9}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(3).to_string(), "")
  assert_eq(text.substring(3, 1).to_string(), "")
}

///|
test "Z3 Unicode case 22" {
  let text = @text.from_string("\u{41}\u{1f600}\u{e9}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(2).to_string(), "\u{e9}")
  assert_eq(text.substring(2, 2147483647).to_string(), "\u{e9}")
}

///|
test "Z3 Unicode case 23" {
  let text = @text.from_string("\u{41}\u{1f600}\u{e9}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(2147483647).to_string(), "")
  assert_eq(text.substring(2147483647, 1).to_string(), "")
}

///|
test "Z3 Unicode case 24" {
  let text = @text.from_string("\u{65e5}\u{672c}\u{8a9e}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(-1).to_string(), "")
  assert_eq(text.substring(-1, 1).to_string(), "")
}

///|
test "Z3 Unicode case 25" {
  let text = @text.from_string("\u{65e5}\u{672c}\u{8a9e}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(0).to_string(), "\u{65e5}")
  assert_eq(text.substring(0, 0).to_string(), "")
}

///|
test "Z3 Unicode case 26" {
  let text = @text.from_string("\u{65e5}\u{672c}\u{8a9e}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(0).to_string(), "\u{65e5}")
  assert_eq(text.substring(0, 1).to_string(), "\u{65e5}")
}

///|
test "Z3 Unicode case 27" {
  let text = @text.from_string("\u{65e5}\u{672c}\u{8a9e}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(1).to_string(), "\u{672c}")
  assert_eq(text.substring(1, 2).to_string(), "\u{672c}\u{8a9e}")
}

///|
test "Z3 Unicode case 28" {
  let text = @text.from_string("\u{65e5}\u{672c}\u{8a9e}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(0).to_string(), "\u{65e5}")
  assert_eq(text.substring(0, -1).to_string(), "")
}

///|
test "Z3 Unicode case 29" {
  let text = @text.from_string("\u{65e5}\u{672c}\u{8a9e}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(3).to_string(), "")
  assert_eq(text.substring(3, 1).to_string(), "")
}

///|
test "Z3 Unicode case 30" {
  let text = @text.from_string("\u{65e5}\u{672c}\u{8a9e}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(2).to_string(), "\u{8a9e}")
  assert_eq(text.substring(2, 2147483647).to_string(), "\u{8a9e}")
}

///|
test "Z3 Unicode case 31" {
  let text = @text.from_string("\u{65e5}\u{672c}\u{8a9e}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(2147483647).to_string(), "")
  assert_eq(text.substring(2147483647, 1).to_string(), "")
}

///|
test "Z3 Unicode case 32" {
  let text = @text.from_string("\u{65}\u{301}").unwrap()
  assert_eq(text.length(), 2)
  assert_eq(text.char_at(-1).to_string(), "")
  assert_eq(text.substring(-1, 1).to_string(), "")
}

///|
test "Z3 Unicode case 33" {
  let text = @text.from_string("\u{65}\u{301}").unwrap()
  assert_eq(text.length(), 2)
  assert_eq(text.char_at(0).to_string(), "\u{65}")
  assert_eq(text.substring(0, 0).to_string(), "")
}

///|
test "Z3 Unicode case 34" {
  let text = @text.from_string("\u{65}\u{301}").unwrap()
  assert_eq(text.length(), 2)
  assert_eq(text.char_at(0).to_string(), "\u{65}")
  assert_eq(text.substring(0, 1).to_string(), "\u{65}")
}

///|
test "Z3 Unicode case 35" {
  let text = @text.from_string("\u{65}\u{301}").unwrap()
  assert_eq(text.length(), 2)
  assert_eq(text.char_at(1).to_string(), "\u{301}")
  assert_eq(text.substring(1, 2).to_string(), "\u{301}")
}

///|
test "Z3 Unicode case 36" {
  let text = @text.from_string("\u{65}\u{301}").unwrap()
  assert_eq(text.length(), 2)
  assert_eq(text.char_at(0).to_string(), "\u{65}")
  assert_eq(text.substring(0, -1).to_string(), "")
}

///|
test "Z3 Unicode case 37" {
  let text = @text.from_string("\u{65}\u{301}").unwrap()
  assert_eq(text.length(), 2)
  assert_eq(text.char_at(2).to_string(), "")
  assert_eq(text.substring(2, 1).to_string(), "")
}

///|
test "Z3 Unicode case 38" {
  let text = @text.from_string("\u{65}\u{301}").unwrap()
  assert_eq(text.length(), 2)
  assert_eq(text.char_at(1).to_string(), "\u{301}")
  assert_eq(text.substring(1, 2147483647).to_string(), "\u{301}")
}

///|
test "Z3 Unicode case 39" {
  let text = @text.from_string("\u{65}\u{301}").unwrap()
  assert_eq(text.length(), 2)
  assert_eq(text.char_at(2147483647).to_string(), "")
  assert_eq(text.substring(2147483647, 1).to_string(), "")
}

///|
test "Z3 Unicode case 40" {
  let text = @text.from_string("\u{0}\u{22}\u{5c}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(-1).to_string(), "")
  assert_eq(text.substring(-1, 1).to_string(), "")
}

///|
test "Z3 Unicode case 41" {
  let text = @text.from_string("\u{0}\u{22}\u{5c}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(0).to_string(), "\u{0}")
  assert_eq(text.substring(0, 0).to_string(), "")
}

///|
test "Z3 Unicode case 42" {
  let text = @text.from_string("\u{0}\u{22}\u{5c}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(0).to_string(), "\u{0}")
  assert_eq(text.substring(0, 1).to_string(), "\u{0}")
}

///|
test "Z3 Unicode case 43" {
  let text = @text.from_string("\u{0}\u{22}\u{5c}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(1).to_string(), "\u{22}")
  assert_eq(text.substring(1, 2).to_string(), "\u{22}\u{5c}")
}

///|
test "Z3 Unicode case 44" {
  let text = @text.from_string("\u{0}\u{22}\u{5c}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(0).to_string(), "\u{0}")
  assert_eq(text.substring(0, -1).to_string(), "")
}

///|
test "Z3 Unicode case 45" {
  let text = @text.from_string("\u{0}\u{22}\u{5c}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(3).to_string(), "")
  assert_eq(text.substring(3, 1).to_string(), "")
}

///|
test "Z3 Unicode case 46" {
  let text = @text.from_string("\u{0}\u{22}\u{5c}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(2).to_string(), "\u{5c}")
  assert_eq(text.substring(2, 2147483647).to_string(), "\u{5c}")
}

///|
test "Z3 Unicode case 47" {
  let text = @text.from_string("\u{0}\u{22}\u{5c}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(2147483647).to_string(), "")
  assert_eq(text.substring(2147483647, 1).to_string(), "")
}

///|
test "Z3 Unicode case 48" {
  let text = @text.from_string("\u{d7ff}\u{e000}\u{ffff}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(-1).to_string(), "")
  assert_eq(text.substring(-1, 1).to_string(), "")
}

///|
test "Z3 Unicode case 49" {
  let text = @text.from_string("\u{d7ff}\u{e000}\u{ffff}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(0).to_string(), "\u{d7ff}")
  assert_eq(text.substring(0, 0).to_string(), "")
}

///|
test "Z3 Unicode case 50" {
  let text = @text.from_string("\u{d7ff}\u{e000}\u{ffff}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(0).to_string(), "\u{d7ff}")
  assert_eq(text.substring(0, 1).to_string(), "\u{d7ff}")
}

///|
test "Z3 Unicode case 51" {
  let text = @text.from_string("\u{d7ff}\u{e000}\u{ffff}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(1).to_string(), "\u{e000}")
  assert_eq(text.substring(1, 2).to_string(), "\u{e000}\u{ffff}")
}

///|
test "Z3 Unicode case 52" {
  let text = @text.from_string("\u{d7ff}\u{e000}\u{ffff}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(0).to_string(), "\u{d7ff}")
  assert_eq(text.substring(0, -1).to_string(), "")
}

///|
test "Z3 Unicode case 53" {
  let text = @text.from_string("\u{d7ff}\u{e000}\u{ffff}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(3).to_string(), "")
  assert_eq(text.substring(3, 1).to_string(), "")
}

///|
test "Z3 Unicode case 54" {
  let text = @text.from_string("\u{d7ff}\u{e000}\u{ffff}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(2).to_string(), "\u{ffff}")
  assert_eq(text.substring(2, 2147483647).to_string(), "\u{ffff}")
}

///|
test "Z3 Unicode case 55" {
  let text = @text.from_string("\u{d7ff}\u{e000}\u{ffff}").unwrap()
  assert_eq(text.length(), 3)
  assert_eq(text.char_at(2147483647).to_string(), "")
  assert_eq(text.substring(2147483647, 1).to_string(), "")
}

///|
test "Z3 Unicode case 56" {
  let text = @text.from_string("\u{10000}\u{2ffff}").unwrap()
  assert_eq(text.length(), 2)
  assert_eq(text.char_at(-1).to_string(), "")
  assert_eq(text.substring(-1, 1).to_string(), "")
}

///|
test "Z3 Unicode case 57" {
  let text = @text.from_string("\u{10000}\u{2ffff}").unwrap()
  assert_eq(text.length(), 2)
  assert_eq(text.char_at(0).to_string(), "\u{10000}")
  assert_eq(text.substring(0, 0).to_string(), "")
}

///|
test "Z3 Unicode case 58" {
  let text = @text.from_string("\u{10000}\u{2ffff}").unwrap()
  assert_eq(text.length(), 2)
  assert_eq(text.char_at(0).to_string(), "\u{10000}")
  assert_eq(text.substring(0, 1).to_string(), "\u{10000}")
}

///|
test "Z3 Unicode case 59" {
  let text = @text.from_string("\u{10000}\u{2ffff}").unwrap()
  assert_eq(text.length(), 2)
  assert_eq(text.char_at(1).to_string(), "\u{2ffff}")
  assert_eq(text.substring(1, 2).to_string(), "\u{2ffff}")
}

///|
test "Z3 Unicode case 60" {
  let text = @text.from_string("\u{10000}\u{2ffff}").unwrap()
  assert_eq(text.length(), 2)
  assert_eq(text.char_at(0).to_string(), "\u{10000}")
  assert_eq(text.substring(0, -1).to_string(), "")
}

///|
test "Z3 Unicode case 61" {
  let text = @text.from_string("\u{10000}\u{2ffff}").unwrap()
  assert_eq(text.length(), 2)
  assert_eq(text.char_at(2).to_string(), "")
  assert_eq(text.substring(2, 1).to_string(), "")
}

///|
test "Z3 Unicode case 62" {
  let text = @text.from_string("\u{10000}\u{2ffff}").unwrap()
  assert_eq(text.length(), 2)
  assert_eq(text.char_at(1).to_string(), "\u{2ffff}")
  assert_eq(text.substring(1, 2147483647).to_string(), "\u{2ffff}")
}

///|
test "Z3 Unicode case 63" {
  let text = @text.from_string("\u{10000}\u{2ffff}").unwrap()
  assert_eq(text.length(), 2)
  assert_eq(text.char_at(2147483647).to_string(), "")
  assert_eq(text.substring(2147483647, 1).to_string(), "")
}

///|
test "Z3 Unicode case 64" {
  let text = @text.from_string("\u{5c}\u{75}\u{7b}\u{34}\u{31}\u{7d}").unwrap()
  assert_eq(text.length(), 6)
  assert_eq(text.char_at(-1).to_string(), "")
  assert_eq(text.substring(-1, 1).to_string(), "")
}

///|
test "Z3 Unicode case 65" {
  let text = @text.from_string("\u{5c}\u{75}\u{7b}\u{34}\u{31}\u{7d}").unwrap()
  assert_eq(text.length(), 6)
  assert_eq(text.char_at(0).to_string(), "\u{5c}")
  assert_eq(text.substring(0, 0).to_string(), "")
}

///|
test "Z3 Unicode case 66" {
  let text = @text.from_string("\u{5c}\u{75}\u{7b}\u{34}\u{31}\u{7d}").unwrap()
  assert_eq(text.length(), 6)
  assert_eq(text.char_at(0).to_string(), "\u{5c}")
  assert_eq(text.substring(0, 1).to_string(), "\u{5c}")
}

///|
test "Z3 Unicode case 67" {
  let text = @text.from_string("\u{5c}\u{75}\u{7b}\u{34}\u{31}\u{7d}").unwrap()
  assert_eq(text.length(), 6)
  assert_eq(text.char_at(1).to_string(), "\u{75}")
  assert_eq(text.substring(1, 2).to_string(), "\u{75}\u{7b}")
}

///|
test "Z3 Unicode case 68" {
  let text = @text.from_string("\u{5c}\u{75}\u{7b}\u{34}\u{31}\u{7d}").unwrap()
  assert_eq(text.length(), 6)
  assert_eq(text.char_at(0).to_string(), "\u{5c}")
  assert_eq(text.substring(0, -1).to_string(), "")
}

///|
test "Z3 Unicode case 69" {
  let text = @text.from_string("\u{5c}\u{75}\u{7b}\u{34}\u{31}\u{7d}").unwrap()
  assert_eq(text.length(), 6)
  assert_eq(text.char_at(6).to_string(), "")
  assert_eq(text.substring(6, 1).to_string(), "")
}

///|
test "Z3 Unicode case 70" {
  let text = @text.from_string("\u{5c}\u{75}\u{7b}\u{34}\u{31}\u{7d}").unwrap()
  assert_eq(text.length(), 6)
  assert_eq(text.char_at(5).to_string(), "\u{7d}")
  assert_eq(text.substring(5, 2147483647).to_string(), "\u{7d}")
}

///|
test "Z3 Unicode case 71" {
  let text = @text.from_string("\u{5c}\u{75}\u{7b}\u{34}\u{31}\u{7d}").unwrap()
  assert_eq(text.length(), 6)
  assert_eq(text.char_at(2147483647).to_string(), "")
  assert_eq(text.substring(2147483647, 1).to_string(), "")
}
