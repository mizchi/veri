///|
pub fn[T] model(stack : Stack[T]) -> @logic_list.List[T] {
  match stack {
    Stack(None) => @logic_list.empty()
    Stack(Some((value, rest))) => @logic_list.cons(value, model(rest))
  }
}

///|
#proof_import("list.List")
fn[T] same(left : T, right : T) -> Bool = "(=)"
