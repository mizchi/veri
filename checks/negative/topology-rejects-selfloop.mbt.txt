///|
// A genuine cycle must also remain logically possible.
fn negative_control() -> Bool where { proof_ensure: accepted => !accepted } {
  let edges : FixedArray[@checker.Edge] = [{ from: 0, to: 0, weight: 0 }]
  check_cycle(edges, [0], [0, 0])
}
