///|
fn third() -> @real.Real {
  @real.div(@real.one(), @real.add(@real.one(), @real.add(@real.one(), @real.one())))
}

///|
pub lemma negative_control() where {
  proof_ensure: @real.within(round64(@ieee754.nearest_even(), third()), third(), @real.zero()),
} {}
